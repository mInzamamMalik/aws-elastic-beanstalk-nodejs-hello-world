1. install elasticbeanstalk cli from here: https://github.com/aws/aws-elastic-beanstalk-cli-setup


2. `sudo eb init`

3. `sudo eb deploy`

4. to add env var `sudo eb setenv KEY1=value1 KEY2=value2` it will be added in beanstalk console dashboard (not in file)